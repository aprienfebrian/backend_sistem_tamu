

generator client {
  provider = "prisma-client-js"
  // output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  Admin
  User
}

enum Status {
  Pending
  Approved
  Selesai
}

model User {
  id        Int     @id @default(autoincrement())
  nama      String
  identitas     String  @unique
  telepon String 
  role Role 
  status Boolean @default(true)
  createdAt DateTime @default(now())

  @@map("users")
}

model Entri {
    id        Int     @id @default(autoincrement())
  tanggal DateTime @default(now())
  nama String
  identitas String
  jumlah_tamu Int @default(0)
  institusi String
  keperluan String
  bertemu String
  status Status
  
@@map("entris")
}